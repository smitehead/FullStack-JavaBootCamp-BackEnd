package com.javajava.project.domain.product.entity;

// 구버전 IMAGE 테이블 엔티티 — 신규 ERD에서는 PRODUCT_IMAGE / REPORT_IMAGE / INQUIRY_IMAGE 로 분리됨
// JPA 매핑 제거 (Hibernate가 IMAGE 테이블을 생성하지 않도록)
// 참조하는 코드가 없으므로 이 파일은 사용되지 않음
public class Image {
}
